/**
 * @file packages/runtime/src/runtime.ts
 * @version 0.11.3
 * @maintainer Axel Elstermann | einfach.design (e2d)
 * @scope Runtime package source code.
 * @description Runtime public API facade and core wiring.
 */

/**
 * Runtime entry (value layer).
 *
 * This file is a minimal stub. The real implementation MUST follow the Runtime Spec in the docs.
 * Keep the public surface small and intentional; expose types via `index.types.ts`.
 */

/**
 * Creates a Runtime instance as defined by the Runtime Spec.
 *
 * Note: This is currently a stub to keep the starter pack buildable. Replace the body with the
 * spec-compliant implementation.
 */
export function createRuntime(): never {
  throw new Error(
    'createRuntime() is not implemented yet. Implement it according to the Runtime Spec.',
  );
}
